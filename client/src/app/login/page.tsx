"use client";

import React, { FC } from "react";
import LoginForm from "./components/LoginForm";

const Login: FC = () => {
  return (
    <body>
      <h2>Login</h2>
      <LoginForm />
    </body>
  );
};

export default Login;
